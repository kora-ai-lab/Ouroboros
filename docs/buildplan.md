BUILD PLAN — Ouroboros
Generated by Ophidian · Phase 5

━━━ SLICE SEQUENCE ━━━

Rule: Slice 0 is scaffold. Slice 1 is fastest path to aha moment. Remaining slices follow dependency order.

  SLICE 0 — Scaffold: project init, Tauri shell, DB, design tokens, empty app shell
  SLICE 1 — Floating Bubble + Basic Chat: bubble UI, input bar, message send/receive (local LLM)
  SLICE 2 — Full Chat Container + Streaming: expanded chat, token streaming, message history
  SLICE 3 — Tool Execution Engine: sandboxed execution, permission model, tool registry
  SLICE 4 — Model Management + BYOK: model downloads, cloud providers, API key vault
  SLICE 5 — Settings + Permissions: settings panel, permission defaults, configuration
  SLICE 6 — Onboarding + Launch: first-run flow, landing page, distribution packages
  SLICE 7 — Polish + Edge Cases: visual refinement, keyboard shortcuts, accessibility pass

━━━ FEATURE REGISTRY ━━━

FEATURE-000
  Name: Project Scaffold + App Shell
  Slice: 0
  Depends on: none
  Files to create/modify:
    db: migration 001 (schema_version), migration 002 (full schema — conversations, messages, api_keys, settings, tool_registry)
    api: Rust commands module scaffold, app state (DB connection pool)
    ui: App Shell (empty), Tailwind config with design tokens, global CSS, type definitions
    design: Tailwind theme extends design-system.md tokens (colors, fonts, spacing, radii, shadows, motion)
    test: cargo test scaffold, Vitest scaffold, Playwright config
  Spec reference: architecture.md — schema, project structure. design-system.md — token implementation.
  DoD:
    - [ ] Tauri project builds (pnpm tauri dev runs without errors)
    - [ ] SQLite database initializes with full schema on first launch
    - [ ] All design tokens available as Tailwind classes (colors, fonts, spacing, radii)
    - [ ] Rust command scaffold working (one test command returns "pong")
    - [ ] TypeScript strict — no errors
    - [ ] cargo test passes
    - [ ] Vitest scaffold test passes
    - [ ] Conventional commit
  Status: [ ]

FEATURE-001
  Name: Floating Bubble + Basic Chat
  Slice: 1
  Depends on: Slice 0
  Files to create/modify:
    db: none (schema already exists)
    api: commands/chat.rs (send_message with local LLM), commands/auth.rs (device ID), events.rs
    ui: bubble/bubble.tsx, bubble/drag-handle.tsx, bubble/bubble-menu.tsx, chat/input-bar.tsx, chat/message-bubble.tsx, chat/streaming-text.tsx
    design: Bubble-specific shadow token (glow), input bar styling
    test: Vitest (bubble component, input bar), Playwright (click bubble, type message, see response)
  Spec reference: prd.md FEATURE-001, FEATURE-002. ux.md — Screen 0 (Bubble), Screen 1 (Input Bar). copy-v1.md — bubble, input bar, chat sections.
  DoD:
    - [ ] Bubble renders as draggable, 44px circle with amber glow on desktop
    - [ ] Click bubble → input bar slides up (250ms, ease.enter)
    - [ ] Type message, Enter → sends to backend, local LLM responds
    - [ ] Response appears above input bar with streaming text
    - [ ] Escape → input bar closes, bubble reappears
    - [ ] Right-click bubble → context menu (New, Settings, Hide, Quit)
    - [ ] All 4 states: loading (skeleton), empty (placeholder + suggestions), error (network/model failed), success (response displayed)
    - [ ] All colors/spacing from design tokens (no hex codes)
    - [ ] All text from copy keys (no inline strings)
    - [ ] Accessible: keyboard, ARIA labels, focus
    - [ ] Tests pass (Vitest + Playwright)
    - [ ] Conventional commits

FEATURE-002
  Name: Full Chat Container + Streaming
  Slice: 2
  Depends on: Slice 1
  Files to create/modify:
    api: commands/chat.rs (conversation CRUD, message history, streaming via events), db/conversations.rs, db/messages.rs
    ui: chat/chat-container.tsx, chat/message-list.tsx, chat/message-bubble.tsx (enhanced), chat/sidebar.tsx, chat/conversation-list.tsx
    test: Vitest (message list, conversation CRUD), Playwright (full chat flow: expand, scroll, sidebar, switch conversations)
  Spec reference: prd.md FEATURE-002. ux.md — Screen 2 (Chat Container). copy-v1.md — chat, sidebar sections.
  DoD:
    - [ ] Chat container expands from input bar (250ms, ease.enter)
    - [ ] Message list scrolls, auto-scrolls to new messages
    - [ ] Streaming tokens render in real-time with cursor indicator
    - [ ] Conversations persisted to SQLite, loadable on restart
    - [ ] Sidebar opens/closes (conversation list)
    - [ ] Conversation CRUD (create, delete, rename, switch)
    - [ ] Scroll-to-bottom button when scrolled up
    - [ ] All 4 states: loading (skeletons), empty (new conversation prompt), error (load failed, stream interrupted), success
    - [ ] Design tokens + copy keys
    - [ ] Accessibility pass
    - [ ] Tests pass
    - [ ] Conventional commits

FEATURE-003
  Name: Tool Execution Engine
  Slice: 3
  Depends on: Slice 2
  Files to create/modify:
    db: tool_registry queries (db/tool_registry.rs)
    api: commands/tools.rs, sandbox/executor.rs, sandbox/filesystem.rs, sandbox/permissions.rs
    ui: tools/permission-dialog.tsx, tools/execution-result.tsx, chat/message-bubble.tsx (tool call integration)
    test: cargo test (sandbox tests), Vitest (permission dialog), Playwright (full tool execution: assistant suggests tool → approve → execute → see result)
  Spec reference: prd.md FEATURE-004. ux.md — Screen 3 (Permission Dialog). copy-v1.md — permission section.
  DoD:
    - [ ] Assistant can suggest tool calls, user can run them
    - [ ] Sandboxed execution: restricted process spawn, FS access limited, network disabled
    - [ ] Permission dialog shows before every execution (allow/deny/always allow)
    - [ ] Tool result rendered inline in chat
    - [ ] Built-in tools: read_file, write_file, list_directory, run_command, search_files
    - [ ] File writes are undoable (backup before write)
    - [ ] All 4 states for permission dialog + execution result
    - [ ] Design tokens + copy keys
    - [ ] Security: no path traversal, no command injection, no sandbox escape
    - [ ] Tests pass (including security test cases)
    - [ ] Conventional commits

FEATURE-004
  Name: Model Management + BYOK
  Slice: 4
  Depends on: Slice 3
  Files to create/modify:
    api: commands/models.rs, llm/cloud.rs (provider adapters), llm/orchestration.rs (model selection, fallback), crypto/mod.rs (AES-256-GCM key encryption)
    ui: settings/model-selector.tsx, settings/api-key-form.tsx, chat/model-indicator.tsx
    test: cargo test (encryption, provider adapters), Vitest (model selector, API key form), Playwright (switch models mid-chat, test cloud model response)
  Spec reference: prd.md FEATURE-003, FEATURE-005. ux.md — Screen 4 (Settings — Models, Providers tabs). copy-v1.md — settings.models, settings.providers, error.auth sections.
  DoD:
    - [ ] Local models downloadable from HuggingFace with progress bar
    - [ ] Cloud providers: OpenAI, Anthropic, Google (API key + OAuth)
    - [ ] API keys encrypted with AES-256-GCM, decrypted only for provider requests
    - [ ] Model switching: user can change model mid-conversation
    - [ ] Hardware detection: GPU vendor, VRAM, recommended model
    - [ ] Model list UI: downloaded + available, download/delete management
    - [ ] API key form: provider picker, key field, optional label, save/remove
    - [ ] Provider status indicators (connected/error)
    - [ ] All 4 states for model download, API key configuration
    - [ ] Design tokens + copy keys
    - [ ] Tests pass
    - [ ] Conventional commits

FEATURE-005
  Name: Settings + Permissions
  Slice: 5
  Depends on: Slice 4
  Files to create/modify:
    api: commands/settings.rs
    ui: settings/settings-panel.tsx (full panel with tabs), settings/general-tab.tsx, settings/models-tab.tsx (enhanced), settings/providers-tab.tsx (enhanced), settings/permissions-tab.tsx
    test: Vitest (settings components), Playwright (full settings flow)
  Spec reference: prd.md — settings. ux.md — Screen 4 (Settings Panel). copy-v1.md — settings section.
  DoD:
    - [ ] Settings panel: modal with sidebar tabs (General, Models, Providers, Permissions)
    - [ ] General: auto-start, hotkey recording, theme, bubble position reset, version
    - [ ] Models: list, download, delete, set default
    - [ ] Providers: add/remove API keys, OAuth connect, status
    - [ ] Permissions: per-tool default (ask/allow/deny), reset all
    - [ ] Unsaved changes detection + prompt
    - [ ] All 4 states per tab
    - [ ] Design tokens + copy keys
    - [ ] Tests pass
    - [ ] Conventional commits

FEATURE-006
  Name: Onboarding + Landing Page
  Slice: 6
  Depends on: Slice 5
  Files to create/modify:
    ui: onboarding/ (welcome, hardware, ready steps), landing page (Next.js/Astro separate project or static site)
    test: Playwright (onboarding flow end-to-end)
  Spec reference: ux.md — Screen 5 (Onboarding). copy-v1.md — onboarding, homepage sections.
  DoD:
    - [ ] First-run onboarding: hardware detection → model recommendation → ready
    - [ ] Model download during onboarding with progress
    - [ ] Final step: bubble is interactive CTA
    - [ ] Landing page: hero, features (3), social proof, download section
    - [ ] /alternatives/openclaw page
    - [ ] OS-detected download buttons
    - [ ] Onboarding < 2 minutes from launch to first message
    - [ ] All 4 states for onboarding steps
    - [ ] Design tokens + copy keys
    - [ ] Tests pass
    - [ ] Conventional commits

FEATURE-007
  Name: Polish + Edge Cases
  Slice: 7
  Depends on: Slice 6
  Files to create/modify:
    all — visual refinement pass, keyboard shortcuts, accessibility audit, edge case hardening
    test: Playwright (accessibility audits, edge cases)
  Spec reference: all ux.md screens — verify compliance. design-system.md — visual audit. copy-v1.md — verify all keys used.
  DoD:
    - [ ] Keyboard shortcuts: Alt+Space (global), Ctrl+N, Ctrl+K, Escape, Tab navigation
    - [ ] Focus states visible on all interactive elements
    - [ ] prefers-reduced-motion support
    - [ ] Screen reader pass on all screens
    - [ ] Dark/light mode (if implemented)
    - [ ] High-DPI rendering verification
    - [ ] Dual monitor behavior
    - [ ] Anti-algorithm guardrails re-check on all screens
    - [ ] Visual regression check against UX specs
    - [ ] Tests pass
    - [ ] Conventional commits

━━━ TOTAL ESTIMATES ━━━
  Slices: 8 (0-7)
  Features: 8 (000-007)
  P0 features covered in: Slices 0-3
  P1 features covered in: Slices 4-7
  Total effort estimate: 4-6 weeks for a full-time solo builder

COMMIT: docs: add buildplan.md, PROGRESS.md [ophidian/phase-5]
TAG: ophidian/phase-5

GATE 5:
✅ Slices sequenced by dependency + aha-moment-first (Bubble + Basic Chat in Slice 1)
✅ Each slice is a complete vertical integration (DB → API → UI → test)
✅ Every feature has DoD checklist with design system and accessibility items
✅ Feature count = 8 for v1 (realistic)
✅ Slice 0 includes design token implementation
✅ Slice 1 is fastest path to proof of value
→ "Build Plan validated. Type OK to begin Phase 6 execution."