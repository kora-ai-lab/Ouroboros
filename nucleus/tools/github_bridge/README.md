# github_bridge

GitHub API integration. Requires GITHUB_TOKEN or GH_TOKEN environment variable.

## Actions
- `list_repos` — list authenticated user's repos
- `create_repo` — create a new repo (private by default)
- `list_issues` — list open issues for a repo
- `create_issue` — create issue with title + body
- `get_readme` — fetch repo README
