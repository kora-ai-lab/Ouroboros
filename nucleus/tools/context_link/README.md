# context_link

Cross-project context retrieval. Searches episodic memory and sessions across all projects for context relevant to a query.

Useful for linking information between projects when a task spans multiple contexts.
