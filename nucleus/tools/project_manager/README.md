# project_manager

Discover, track, and switch between projects.

## Actions
- `list` — list known projects
- `add` — add a project by name and path
- `switch` — switch current project context
- `sync` — index project structure
- `discover` — auto-discover git repos in workspace
