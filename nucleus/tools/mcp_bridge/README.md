# mcp_bridge

Generic bridge for configured Model Context Protocol servers. It exposes server discovery, tool discovery/calls, resource discovery/reads, and prompt discovery without embedding domain-specific tools in the kernel.
